package cs3500.music.util;

/**
 * Created by Abinader on 11/11/16.
 * This is an enum for directions that is used for keyboard scrolling, it is in the util package
 * rather than being an inner class because both the controller and the gui need to use this enum.
 */
public enum Direction {
  LEFT, RIGHT, UP, DOWN
}
